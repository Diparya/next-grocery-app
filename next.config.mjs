/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['m.media-amazon.com',"befreshcorp.net","static.toiimg.com","assets.clevelandclinic.org","img.freepik.com"],
      },
};

export default nextConfig;
